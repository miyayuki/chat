<h2>edit your group</h2>

<ul class="groups">
	<%= form_for(@group, :url=>{:action => 'update'}, :html=>{:method =>:put}) do %>
		<div>
		グループ名<%= @group.name %>
		<br><%= text_field "group", :name %>
		</div>
		<div><%= submit_tag '変更' %></div>
	<% end %>

</ul>
