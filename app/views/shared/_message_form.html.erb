<%= form_for(@message, :url=>{:action=>:create, :controller=>:messages, :group_id=>@group.id}) do |f| %>
	<div class="field">
		チャットに投稿<br>
		<%= f.text_area :content, placeholder: "メッセージを書いてください"%>
	</div>
	<%= f.submit "送信", class: "btn btn-large btn-primary" %>
<% end %>
